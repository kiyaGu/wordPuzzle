const conWordCollection = require('../constructWordCollection');
describe('The word collection constructor', () => {
    it('should construct words ', () => {
        // expect(conWordCollection()).toBe(typeOf conWordCollection() == "function");
    })
    it('given an array it should construct a json object', () => {

    })
});